route:
  receiver: default
  group_by: [alertname, severity]
  group_wait: 15s
  group_interval: 1m
  repeat_interval: 4h

receivers:
  - name: default
    webhook_configs:
      - url: http://127.0.0.1:5001/
        send_resolved: true
